#!/bin/bash
# Array of SQL files in order
sql_files=(
    "schema.sql"
    "accounts.sql"
    "friends.sql"
    "solutions.sql"
    "comments.sql"
    "problems.sql"
    "account_solved_problem.sql"
    "submissions.sql"
    "starred.sql"
)

# Loop through the files and execute them in order
for sql_file in "${sql_files[@]}"; do
    echo "Executing $sql_file..."
    cat "schemas/$sql_file" >> init.sql
done

# Insert sample data
#psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" -f "sample.sql"

cat data/sample.sql >> init.sql 
