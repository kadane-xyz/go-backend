version: "2"
sql:
- schema: schema.sql
  queries: query.sql
  engine: postgresql
  gen:
    go:
      package: sql
      out: sql
      sql_package: pgx/v5
      emit_json_tags: true
      json_tags_case_style: camel
      emit_prepared_queries: false
      emit_interface: false